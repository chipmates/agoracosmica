# Same layout as client/.npmrc. The client build script installs this
# package with those settings inherited; a standalone install must match.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
