---
// A page for each sonnet the collection page hosts, numbered as the 1609
// Quarto numbers them, so /sonnets/18/ is the page for Sonnet 18.

import PoemPage from '../../../../components/PoemPage.astro';
import { poemsByPoet } from '../../../../data/poems/index.mjs';

export function getStaticPaths() {
  return poemsByPoet('shakespeare').map((poem) => ({
    params: { sonnet: poem.slug },
    props: { poem },
  }));
}

const { poem } = Astro.props;
---
<PoemPage poem={poem} />
