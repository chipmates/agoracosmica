version: 2

updates:
  # One entry per package directory, since the repo has no pnpm workspace root
  # and every package carries its own lockfile. Dependabot opens a separate PR
  # per directory.
  - package-ecosystem: "npm"
    directories:
      - "/client"
      - "/marketing"
      - "/workers/llm-proxy"
      - "/workers/audio-proxy"
      - "/workers/community"
      - "/workers/stats"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    ignore:
      # 6.x needs Vite 8 as its peer and imports vite/internal, which Vite 7
      # does not ship. Lift this with the Vite 8 migration.
      - dependency-name: "@vitejs/plugin-react"
        update-types: ["version-update:semver-major"]
    groups:
      # Minor and patch travel together; anything major falls outside the group
      # and gets its own PR, so a breaking bump is reviewed on its own.
      minor-and-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    groups:
      actions-minor-and-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
